spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://39.106.117.150:3306/h2movie?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: pscxtc98v*
  application:
    name: provider-pall
server:
  port: 8340

eureka:
  client:
    service-url:
      defaultZone: http://user:password123@127.0.0.1:8761/eureka/
  instance:
    prefer-ip-address: true

mybatis:
  config-location: classpath:config/mybatis-config.xml
  mapper-locations: classpath:mapper/*.xml

feign:
  client:
    config:
      default:
        connectTimeout: 1000
        readTimeout: 1000
  hystrix:
    enabled: true
tx-lcn:
  client:
    manager-address: 127.0.0.1:8070